# The following variable can be used to determine if StarNet AI - Planetary Diversity compatibility patch is properly loaded (overwriting StarNet). Always trigger check with equal. It does not check if other mods are overwriting the patch.
@str_is_planetary_diversity_patch_loaded = 0